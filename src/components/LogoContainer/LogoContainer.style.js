import styled from 'styled-components'
import { logoWidth } from '../../config/sizes'

const Container = styled.div`
  width: ${logoWidth}px;
  height: ${logoWidth}px;
  margin-left: auto;
  margin-right: auto;
  position: relative;
`

export { Container }
